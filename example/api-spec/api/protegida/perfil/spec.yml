description: Perfil del usuario autenticado. Hereda el auth bearer (y behavior) de protegida.
method: GET
cases:
  ok:
    response: response-200.json
    status: 200
dynamic:
  "data.sessionId": "{{uuid}}"
  "timestamp": "{{now}}"
