description: Tarifas públicas. OVERRIDE — desactiva el guard heredado de protegida.
method: GET
auth: none      # override: anula el bearer heredado (como requireAuth: false)
cases:
  ok:
    response: response-200.json
    status: 200
